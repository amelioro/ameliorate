import { css } from "@emotion/react";
import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const WorkspaceBox = styled(Box)`
  height: 100%;
  position: relative;
`;

export const workspaceStyles = css`
  body {
    overflow: hidden;
  }
`;
